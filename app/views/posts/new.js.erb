<% if @post.parent_id? %>
    $('#post_<%= @post.parent_id %> .add_comment').html(
            '<%= escape_javascript(
                    render(partial: 'posts/form')
            ) %>').slideDown();
<% end %>