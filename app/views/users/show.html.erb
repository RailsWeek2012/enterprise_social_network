<div class="btn-group actions" xmlns="http://www.w3.org/1999/html">
  <%=  link_to users_path, class: "btn" do %>
    <i class="icon-chevron-left"></i> Members
  <% end %>
  <% if current_user == @user %>
    <%=  link_to edit_user_path(@user), class: "btn" do %>
      <i class="icon-pencil"></i> Edit
    <% end %>
  <% end %>
</div>
<div class="well profile">
    <% if @user == @user.company.owner %>
        <div class="owner" title="Owner"><i class="icon-star"></i></div>
    <% end %>
    <div class="image">
      <%= gravatar_image_tag(@user.email, alt: @user.full_name, gravatar: { size: 100 }) %>
    </div>
    <h1><%= @user.full_name %></h1>

    <p>
      <b>Email:</b>
      <%= @user.email %>
    </p>

    <p>
      <b>Company:</b>
      <%= company_link @user.company %>
    </p>
</div>
